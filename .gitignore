join-command
.vagrant
.terraform*
backend.hcl
nodes
.DS_Store
__pycache__/
*.pyc

# tfstate (전역)
*.tfstate
*.tfstate.*
terraform.tfvars

# infra-do
infra-do/kubernetes/.kubeconfig

# infra-oracle
infra-oracle/.kubeconfig
infra-oracle/.node-token

.omc

# public Travel dashboard keeps raw calendar/geocode state out of dist
infra-aws-static-sites/sites/travel/data/*.json

# Generated only by the protected gateway deployment step; it contains the
# browser-visible, domain-restricted Mapbox public token and must never be committed.
infra-aws-static-sites/sites/safety-map/dist/runtime-map-config.js
infra-aws-static-sites/sites/safety-map-experiment-03/dist/runtime-map-config.js
